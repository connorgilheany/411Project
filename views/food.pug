extends layout

block content
  body
    div.container(style='margin-top:70px')
      div.row
        div.col-lg-12
          h1 Cook Something
          p Search for recipes

          #form
            form(action = "/food", method="POST", name ="search-food")
              div.input
                  input(type="text", name="query")
              div.actions
                  input.btn.btn-info(type='submit', value='Search')

    <br>
    div.container
        if userQuery
          p Top 10 Results for #{userQuery}
        ul.list-group
        for item in results
          li.list-group-item: a(href=item.url, target="_blank") #{item.name}
