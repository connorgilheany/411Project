extends layout

block content
  h1= title
  p Book stuff #{books}
  p Welcome to #{title}
  form(name="search-books", method="post")
        div.input
            span.label Book Title 
            input(type="text", name="title")
        div.actions
            input(type="submit", value="Search")
