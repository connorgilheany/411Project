extends layout

block content
  h1=title

  div(class="InsideContent")
    iframe(width='1120', height='630', src=videoURL, frameborder="0", allowfullscreen='allowfullscreen')
  div(class="InsideContent")
    span(id="blackText") Current Trending Videos
    for item in videoList
        include video.pug
